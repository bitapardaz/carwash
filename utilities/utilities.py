

#introduce a worker given a pending order
def find_worker(pending_order):
    #return Worker
    #logic: first search through the workers that reside in the same region of
    #the order. If failed, search through the adjacent regions. 
    pass

#introduce a list of workers given an order
#that cannot be fulfilled by its current worker
def replace_worker(order):
    #return [worker]
    pass
